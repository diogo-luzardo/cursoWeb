const _ = require('lodash')
setInterval(() => console.log(_.random(1, 1000)), 2000)
/*O código acima usa o setInterval para pegar um intervalo com a biblioteca random do modulo lodash
com o intervalo de 1 a 1000, com o tempo de 2 segundos representado por 2000. */

